[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["cd python-api && pip install -r requirements.txt"]

[start]
cmd = "export PYTHONPATH=/app/keiba:$PYTHONPATH && cd python-api && uvicorn main:app --host 0.0.0.0 --port $PORT"
